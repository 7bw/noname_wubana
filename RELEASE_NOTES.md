## 五班阿扩展

五班同学主题 DIY 武将扩展（基于无名杀）。共 **57 名武将**，均为同学梗自制。

### 本次更新（v1.3.4）

**Bug 修复**
- **游杜时宇·延迟**：v1.3.2 的 `useCardCancelled` 方案仍然会导致同样的静默卡死。彻底放弃拦截真实 `useCard` 事件这条路径，改为用 `mod.cardEnabled` 让你出牌阶段的手牌无法正常使用，配合 v1.2 原版已验证可用的主动技选牌逻辑达到等价效果（可反复发动，选一张手牌盖置），完全不再触碰 `useCard`/`useCardBegin`/`cancel()`
- **赤脚大仙·杜时宇·脚气**：找到真正根因——`getParent(name)` 在不传 `forced: true` 时，找不到匹配祖先会返回一个真值的空对象 `{}` 而非 `undefined`，导致"是否发生在弃牌阶段"的判断形同虚设，出牌阶段打出的牌也被误算进弃牌数。已修正为传入 `forced: true`，并补充了牌本身的 `type` 校验
- **中路杀神·许盛杰·雨神**：视为使用【水淹七军】后，作为代价的"雨"没有被移出扩展区，导致可以无限打出；已在构造视为牌前手动将其移入弃牌堆

**同类 bug 排查**：脚气的根因（`getParent` 不传 `forced: true` 时非匹配返回真值空对象）是引擎级别的隐藏陷阱，逐一排查了全扩展内所有 `getParent` 调用，额外修复了 5 处同款问题（其中 2 处有实际崩溃风险）：
- 游徐家澍·篱下：判断条件形同虚设；非出牌阶段触发时 `content` 会直接崩溃
- 游李博为·火把：经"回合结束阶段"分支触发时会崩溃
- 工宋轶健·思人（子技能）：回合外用基本牌也会误触发下一张基本牌的加成
- 工宋轶健·夜班：判断条件形同虚设（实际影响较小，`getHistory` 本身按回合分桶）
- 游杜时宇（管乐队）·击鼓：边缘情况下连续同类型牌判断可能失效（风险很低）

### 历史更新（v1.3.2）

**Bug 修复**（继续修复 v1.3 新增武将的实机问题）
- **游杜时宇·延迟**：v1.3.1 的修复方式本身仍有问题——`trigger.cancel()` 会强制结束仍在执行中的祖先触发事件，若在同一分支里（不论前后）再 `await` 一个自身会产生新触发级联的引擎事件（如 `addToExpansion` 内部创建的 `lose` 子事件，可被任意角色的 `loseBegin`/`loseAfter` 监听到），会与被强制结束的祖先上下文冲突，导致静默卡死（不报错、控制台无输出）。现改为：`useCardBegin` 里只做同步的取消操作，实际的盖牌动作挪到 `cancel()` 自身触发的 `useCardCancelled`（一次全新的、不嵌套的触发流程）中执行，参考标准包"方天画戟"技能里已有的 `useCardCancelled` 用法
- **赤脚大仙·杜时宇·脚气**：`getHistory` 的类型错误修复后，暴露出下一行代码里另一个独立的笔误 `game.asyncDoAsyncInOrder`（引擎里根本不存在这个方法），已改为正确的 `game.doAsyncInOrder`
- **中路杀神·许盛杰·雨神**：扩展区（`position: "x"`）的牌无法通过声明式的 `enable: "chooseToUse"` + `filterCard`/`viewAs` 选牌 UI 弹出，导致点击后没有牌可选；已改为和"放箭"（工黄彦瑞）一样的手动选牌实现
- **非洲皇帝·刘阳河·宠电宝**：v1.3.1 只修了卡名拼写，逻辑本身仍有问题——用 `player.useCard(...)` 让展示的手牌"视为兵粮寸断"只会触发效果，不会真正把这张牌移动到目标的判定区，导致牌同时贴在目标身上、又还留在自己手里；已改用 `addJudge` 直接将牌移动并置入目标判定区

### 历史更新（v1.3.1）

**Bug 修复**（均为 v1.3 新增武将的实现问题，实机测试中发现）
- **非洲皇帝·刘阳河·宠电宝**：卡名写错（`bingliangcunduan`），导致视为使用【兵粮寸断】时直接崩溃；已改为正确的内部卡名 `bingliang`
- **装逼狂魔·高雯·我从来不装逼**：主动技错误地依赖了一个引擎里根本不会被调用的 `cost` 步骤来选目标（`cost` 仅对触发技生效），导致目标恒为空、技能报错；已改为主动技应有的声明式目标选择
- **赤脚大仙·杜时宇·脚气**：读取历史记录时传了一个不存在的类型（`"discard"`），导致遍历时崩溃；已改为正确的 `"lose"`
- **游杜时宇·延迟**：修正为锁定技后，因 `trigger.cancel()` 调用时机不对（放在了异步操作之前，而非之后），导致自己回合内卡死、且间接影响到其他回合外的响应（如出闪）；已调整调用顺序并收紧了"是否为本人回合"的判定，恢复正常

### 历史更新（v1.3）

**新增势力与武将**
- 新增 **神 / 师 / 学** 三个势力，武将势力体系统一为六个势力：**研 / 工 / 游 / 神 / 师 / 学**（研=3、工=2、游=4、神=10、师=7、学=31，共57名）
- "神"势力新增 5 名武将：装逼狂魔·高雯（装逼·我从来不装逼）、中路杀神·许盛杰（祈雨·雨神·预知）、赤脚大仙·杜时宇（脱鞋·赤脚·脚气）、非洲皇帝·刘阳河（宠电宝·非洲血统）、斗战胜佛·何智昭（佛心·佛光普照）
- "学"势力新增 1 名武将：刘子平（打响指·在梦境中·惊醒）

**改名**
- 工大黄 → **工黄彦瑞**
- mym → **茅艳梅**
- 斗战胜佛何智沼 → **斗战胜佛何智昭**

**性别重新设定**
- 仅蔡晶君、高雯、田雨竹、徐薇、杜任娟、杨雨晨、茅艳梅、装逼狂魔·高雯为女性，其余武将均为男性（含此前设定有误的朱敏惠、戚知悦，已改回男性；杨雨晨、茅艳梅改为女性）

**武将调整**
- 研徐家澍体力上限 3 → 4
- 工宋轶健体力上限 3 → 4
- **游徐家澍·篱下**：去掉"使用锦囊牌后结束出牌阶段"的限制，现在仅保留"出牌阶段至多使用X张牌（X为体力值）"
- **研李博为·向阳**：红色【杀】视为【火杀】时新增无视距离限制

**技能 Bug 修复**
- **神黄彦瑞·借箭**：补上"被其他角色单独指定为目标"的判定，此前自己对自己使用牌也会误触发
- **神宋轶健·摸鱼**：修正阶段跳过的触发时机（原来在阶段已经开始后才取消，导致跳过实际从未生效）；**加班**同步改为"摸X张"（原为"摸X+1张"，与摸鱼失效时的表现不一致）
- **工宋轶健·夜班**：去掉了会阻止额外回合链式发动的多余判断，现在只要额外回合中依旧满足条件，就能连续发动、持续进行额外回合
- **游宋轶健·赤史**：将"智囊牌"范围从一大串锦囊收窄为过河拆桥、无懈可击、无中生有三张，不再对其他锦囊牌的使用误触发摸牌
- **游宋轶健·同质**：记录的牌名现在只有本人客户端可见，其他玩家只能看到"已暗置"的提示，看不到具体牌名
- **游杜时宇·延迟**：由"出牌阶段可选择发动的主动技"改为真正的锁定技，出牌阶段使用手牌会被自动拦截、盖置，不再需要玩家主动选择触发
- **刘阳河·界限突破·摄影**：修正"造成伤害后归还技能"分支的触发角色判定错误（此前该分支永远无法正确触发）；新增对被动触发技的"使用后归还"支持（原实现只对主动技生效）
- **杜任娟·发型崩坏**：改为仅在自己回合结束时结算，且体力值只需为场上最高（不再要求"唯一最高"）
- **许盛杰(基础版)/徐施舟·修仙**：修正因误用内部标记导致技能"不询问、直接自动发动"的 bug；同一文件内因相同笔误导致的另外 5 处技能（邱昊嵘·模仿、邱昊嵘·新生、宋轶健·学委、徐家澍·cool、宋沼·装逼）一并修复
- **宋轶健(基础版)·不萎**：改用更可靠的判定方式，确保回合内、外失去全部手牌时都能正确触发
- **工黄彦瑞·睹物**：体力上限改为与"奶"的当前数量动态挂钩，"奶"被移除时（思人/出差/技能被夺）体力上限相应减少，而不再是获得时单方向永久+1
- **游徐家澍·黑人拍皮球**：修正内部命名与实际生成的子技能名不一致的问题，此前该技能完全无法触发

### 历史更新（v1.2）
- 新增 **研 / 工 / 游** 三个新势力，共 **9 名武将 / 28 个技能**：
  - **研**：研杜时宇（实验·百川·勃朗）、研徐家澍（小本生意·转运·走私·FBI）、研李博为（苦寒·自闭·向阳）
  - **工**：工大黄（睹物·思人·出差）、工宋轶健（调班·白班·夜班）
  - **游**：游宋轶健（同质·赤史·战术）、游李博为（长夜·火把·会赢的）、游杜时宇（延迟·巧匠·赢你妈）、游徐家澍（创新·黑人拍皮球·篱下）
- 含使命技、协力技、势力技（阳/阴）等新机制。

### 下载
- **Windows**：`noname.Setup.*.exe` — 安装版，已内置全部素材与扩展，安装后即可游玩。
- **安卓**：`noname-wuban-test-*.apk` — **非官方测试包**，需侧载安装；体积较大（约 1.2GB，自带素材可离线玩）。
- `noname.full.zip` / `noname.core.zip` — 完整网页包 / 纯代码包（进阶/开发用途）。

### 说明
- 扩展**默认已开启**，进入游戏后在选将界面即可看到「五班阿」武将。
- 安卓包为**调试签名的测试版**，仅供和朋友试玩，**非任何官方发布**。
- 本扩展与游戏本体均基于 GPLv3 开源，源自无名杀（libnoname/noname）。
